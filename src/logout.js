import React from "react";

export default function Logout() {
    return (
        <div className="logout">
            <a href="/logout">
                <button> Log Out </button>
            </a>
        </div>
    );
}
